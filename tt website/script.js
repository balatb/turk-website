// Optional: Add any JavaScript for animations or interactions.// Example: Add smooth scrolling effect when clicking on section linksdocument.querySelectorAll('a[href^="#"]').forEach(anchor => {    anchor.addEventListener('click', function (e) {        e.preventDefault();        document.querySelector(this.getAttribute('href')).scrollIntoView({            behavior: 'smooth'        });    });});